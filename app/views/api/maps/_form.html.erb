<%= form_with(model: api_map, local: true) do |form| %>
  <% if api_map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_map.errors.count, "error") %> prohibited this api_map from being saved:</h2>

      <ul>
      <% api_map.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :lat %>
    <%= form.text_field :lat %>
  </div>

  <div class="field">
    <%= form.label :long %>
    <%= form.text_field :long %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
