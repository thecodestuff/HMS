<h2>List of Nurses
  <span class="badge badge-pill badge-info"><%= User.by_role(:Nurse).count %></span>
</h2>
<% @users = @nurses %>
<%= render 'table' %>
<div class="paginationbox">
  <%= paginate @physicians, param_name: :nurse_tab, theme: 'twitter-bootstrap-4' %>
</div>