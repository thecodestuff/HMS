function updateFlash(){
  $('.notice').html("Appointment sheduled successfully...")
}

function renderAppointmentTable(){
  $("#appointmentsTable tbody").append('<%= j render 'table_row', appointment:@appointment.last %>');
}

$("#popup").modal('hide');
renderAppointmentTable() ;
updateFlash();