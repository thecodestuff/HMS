<tr>
  <td><%= invoice.id %></td>
  <td><%= invoice.patient_id %></td>
  <td><%= invoice.transactionId %></td>
  <td><%= invoice.status %></td>
  <td><%= invoice.amount %><b> &#8377;</b></td>
  <td>
    <a href="<%= admin_invoice_status_path(invoice) %>" class="btn btn-warning">Done</a>
    <a href="<%= admin_invoice_path invoice %>" class="btn btn-primary">Reciept</a>
    <a href="#" class="btn btn-success">Download</a>
    <a href="#" class="btn btn-warning">Print</a>
    <%= link_to 'Create PDF document', admin_invoice_path(invoice, :format => :pdf) %>
  </td>
</tr>